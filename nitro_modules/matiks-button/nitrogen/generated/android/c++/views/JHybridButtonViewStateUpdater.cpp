///
/// JHybridButtonViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridButtonViewStateUpdater.hpp"
#include "views/HybridButtonViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::matiksbutton::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridButtonViewState>;

void JHybridButtonViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridButtonViewSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridButtonViewSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridButtonViewState& data = concreteState->getData();
  const std::optional<HybridButtonViewProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridButtonViewState's data doesn't contain any props!");
  }
  const HybridButtonViewProps& props = maybeProps.value();
  if (props.title.isDirty) {
    view->setTitle(props.title.value);
    // TODO: Set isDirty = false
  }
  if (props.onPress.isDirty) {
    view->setOnPress(props.onPress.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridButtonViewSpec> shared = javaView->cthis()->shared_cast<JHybridButtonViewSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::matiksbutton::views
