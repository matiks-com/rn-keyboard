///
/// HybridButtonViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridButtonViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::matiksbutton::views {

  extern const char HybridButtonViewComponentName[] = "ButtonView";

  HybridButtonViewProps::HybridButtonViewProps(const react::PropsParserContext& context,
                                               const HybridButtonViewProps& sourceProps,
                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    title([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("title", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.title;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.title);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ButtonView.title: ") + exc.what());
      }
    }()),
    onPress([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onPress", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onPress;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onPress);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ButtonView.onPress: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridButtonViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridButtonViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ButtonView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridButtonViewProps::HybridButtonViewProps(const HybridButtonViewProps& other):
    react::ViewProps(),
    title(other.title),
    onPress(other.onPress),
    hybridRef(other.hybridRef) { }

  bool HybridButtonViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("title"): return true;
      case hashString("onPress"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridButtonViewComponentDescriptor::HybridButtonViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridButtonViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                      const std::shared_ptr<const react::Props>& props,
                                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridButtonViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridButtonViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridButtonViewShadowNode&>(shadowNode);
    const HybridButtonViewProps& props = concreteShadowNode.getConcreteProps();
    HybridButtonViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::matiksbutton::views
