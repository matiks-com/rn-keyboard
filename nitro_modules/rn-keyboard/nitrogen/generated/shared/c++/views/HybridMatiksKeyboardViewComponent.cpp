///
/// HybridMatiksKeyboardViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridMatiksKeyboardViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::rnkeyboard::views {

  extern const char HybridMatiksKeyboardViewComponentName[] = "MatiksKeyboardView";

  HybridMatiksKeyboardViewProps::HybridMatiksKeyboardViewProps(const react::PropsParserContext& context,
                                                               const HybridMatiksKeyboardViewProps& sourceProps,
                                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    customKeyboardType([&]() -> CachedProp<std::optional<KEYBOARD_TYPE>> {
      try {
        const react::RawValue* rawValue = rawProps.at("customKeyboardType", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.customKeyboardType;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<KEYBOARD_TYPE>>::fromRawValue(*runtime, value, sourceProps.customKeyboardType);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MatiksKeyboardView.customKeyboardType: ") + exc.what());
      }
    }()),
    keyboardLayout([&]() -> CachedProp<std::optional<KEYBOARD_LAYOUT>> {
      try {
        const react::RawValue* rawValue = rawProps.at("keyboardLayout", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.keyboardLayout;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<KEYBOARD_LAYOUT>>::fromRawValue(*runtime, value, sourceProps.keyboardLayout);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MatiksKeyboardView.keyboardLayout: ") + exc.what());
      }
    }()),
    onKeyInput([&]() -> CachedProp<std::optional<std::function<void(const KeyInputEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onKeyInput", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onKeyInput;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const KeyInputEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onKeyInput);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MatiksKeyboardView.onKeyInput: ") + exc.what());
      }
    }()),
    onDelete([&]() -> CachedProp<std::optional<std::function<void(const DeleteEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onDelete", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onDelete;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const DeleteEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onDelete);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MatiksKeyboardView.onDelete: ") + exc.what());
      }
    }()),
    hapticsEnabled([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hapticsEnabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hapticsEnabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.hapticsEnabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MatiksKeyboardView.hapticsEnabled: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMatiksKeyboardViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMatiksKeyboardViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MatiksKeyboardView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridMatiksKeyboardViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("customKeyboardType"): return true;
      case hashString("keyboardLayout"): return true;
      case hashString("onKeyInput"): return true;
      case hashString("onDelete"): return true;
      case hashString("hapticsEnabled"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridMatiksKeyboardViewComponentDescriptor::HybridMatiksKeyboardViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridMatiksKeyboardViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                              const std::shared_ptr<const react::Props>& props,
                                                                                              react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridMatiksKeyboardViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridMatiksKeyboardViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridMatiksKeyboardViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridMatiksKeyboardViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridMatiksKeyboardViewProps>& props = std::const_pointer_cast<HybridMatiksKeyboardViewProps>(constProps);
    HybridMatiksKeyboardViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::rnkeyboard::views
