///
/// HybridMatiksKeyboardViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridMatiksKeyboardViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridMatiksKeyboardViewSpecSwift.hpp"
#import "NitroRnKeyboard-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::rnkeyboard;
using namespace margelo::nitro::rnkeyboard::views;

/**
 * Represents the React Native View holder for the Nitro "MatiksKeyboardView" HybridView.
 */
@interface HybridMatiksKeyboardViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridMatiksKeyboardViewComponent {
  std::shared_ptr<HybridMatiksKeyboardViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridMatiksKeyboardViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridMatiksKeyboardViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridMatiksKeyboardViewSpec> hybridView = NitroRnKeyboard::NitroRnKeyboardAutolinking::createMatiksKeyboardView();
    _hybridView = std::dynamic_pointer_cast<HybridMatiksKeyboardViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroRnKeyboard::HybridMatiksKeyboardViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridMatiksKeyboardViewProps const>(props);
  auto& newViewProps = const_cast<HybridMatiksKeyboardViewProps&>(newViewPropsConst);
  NitroRnKeyboard::HybridMatiksKeyboardViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // customKeyboardType: optional
  if (newViewProps.customKeyboardType.isDirty) {
    swiftPart.setCustomKeyboardType(newViewProps.customKeyboardType.value);
    newViewProps.customKeyboardType.isDirty = false;
  }
  // keyboardLayout: optional
  if (newViewProps.keyboardLayout.isDirty) {
    swiftPart.setKeyboardLayout(newViewProps.keyboardLayout.value);
    newViewProps.keyboardLayout.isDirty = false;
  }
  // onKeyInput: optional
  if (newViewProps.onKeyInput.isDirty) {
    swiftPart.setOnKeyInput(newViewProps.onKeyInput.value);
    newViewProps.onKeyInput.isDirty = false;
  }
  // onDelete: optional
  if (newViewProps.onDelete.isDirty) {
    swiftPart.setOnDelete(newViewProps.onDelete.value);
    newViewProps.onDelete.isDirty = false;
  }
  // hapticsEnabled: optional
  if (newViewProps.hapticsEnabled.isDirty) {
    swiftPart.setHapticsEnabled(newViewProps.hapticsEnabled.value);
    newViewProps.hapticsEnabled.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroRnKeyboard::NitroRnKeyboardAutolinking::isMatiksKeyboardViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroRnKeyboard::HybridMatiksKeyboardViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

@end
